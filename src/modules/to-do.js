// Model for a To-Do

const ToDo = () => {
    let title = "" //Placeholder will be in HTML
    let description = "" //Placeholder will be in HTML
    let dueDate = "" // will implement a time library later.
    let priority = 0 // maybe an ENUM that represents variables by priority
}

export default ToDo;